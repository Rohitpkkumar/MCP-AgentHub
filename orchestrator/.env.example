# Local dfx replica host (default used by dfx)
IC_HOST=http://127.0.0.1:4943

# Path to the candid (.did) file for the registry canister (relative to repo root)
REGISTRY_DID_PATH=canisters/registery/registery_backend/registery_backend.did

# Path to the dfx local canister IDs
DFX_IDS_PATH=.dfx/local/canister_ids.json

# Name of your canister in dfx.json as used earlier
REGISTRY_CANISTER_NAME=registery_backend

# MCP Endpoint
MCP_ENDPOINT=http://localhost:9000
